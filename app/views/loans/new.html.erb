<div class="header">
	<h1>Create loan</h1>
	<h2>Create a loan that can be funded with BTC</h2>
</div>

<div class="content">
	<%= form_for(@loan,
	html: { class: "pure-form pure-form-aligned" }) do |f| %>
		<fieldset>

			<div class="pure-control-group">
				<%= f.label :amount %>
				<%= f.number_field :amount %> BTC
			</div>

			<div class="pure-control-group">
				<%= f.label :proposal %>
				<%= f.text_area :proposal, size: "32x8" %>
			</div>

			<div class="pure-controls">
				<%= f.submit "Create loan", :class => "pure-button pure-button-primary" %>
			</div>
		</fieldset>
	<% end %>
</div>
